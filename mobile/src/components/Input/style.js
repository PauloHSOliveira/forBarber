import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    input: {
        marginTop: 30,
        height: 50,
    },

    icon: {
        alignSelf: 'flex-end',
        zIndex: 5,
        marginTop: -36,
        marginRight: 5,
    },
});

export default Styles;
